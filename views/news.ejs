<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/header") %>
    <title>行政院即時新聞閱讀器</title>
  </head>
  <body>
    <%- include ("partials/nav") %>
    <h1>行政院OpenAPI現在不能用啦...</h1>
    <form
      class="search-form"
      action="/profile/news"
      method="get"
      style="margin-bottom: 2rem"
    >
      <input
        type="text"
        name="q"
        class="search-input"
        placeholder="搜尋文章標題或內容"
        value="<%= typeof query !== 'undefined' ? query : '' %>"
        autocomplete="off"
      />
      <button type="submit" class="search-btn">搜尋</button>
    </form>
    <!-- 文章列表區塊 -->
    <% if (posts.length > 0) { %>
    <section class="posts-section">
      <h2 class="posts-title">本平台上的文章</h2>

      <div class="posts-grid">
        <% for (let i = 0; i < posts.length; i++) { %>
        <article class="post-card">
          <div class="post-body">
            <h3 class="post-title"><%= posts[i].title %></h3>
            <p class="post-content"><%= posts[i].content %></p>
            <div class="post-footer">
              <span class="post-author">作者：<%= posts[i].author.name %></span>
              <time class="post-date">發表時間：<%= posts[i].date %></time>
            </div>
          </div>
        </article>
        <% } %>
      </div>
    </section>
    <% } else if (posts.length === 0) { %>
    <p style="color: #888; text-align: center">查無符合條件的文章。</p>
    <% } %>
  </body>
</html>
